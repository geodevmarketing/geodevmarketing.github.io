// Scripts globaux pour le site CNCT

document.addEventListener("DOMContentLoaded", () => {
    console.log("CNCT Project : scripts globaux chargés.");
});

// Fonction pour le bouton retour
function goBack() {
    window.history.back();
}
